
class Experiments_Settings:

    def __init__(self, within_datasets, between_datasets, within_domains, between_domains, all_datasets, all_domains) -> None:
        self.within_datasets = within_datasets
        self.between_datasets = between_datasets
        self.within_domains = within_domains
        self.between_domains = between_domains
        self.all_datasets = all_datasets
        self.all_domains = all_domains
